<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- FR-19's expiry notifications. Runtime-requested on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Rebuilds the expiry alarms after a restart. Enforcement itself does not need
         this: grantEndsAt survives in SharedPreferences and the watcher re-reads it. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- The user can refuse this on Android 12+, and on 14+ it is not granted by
         default. GrantAlarms degrades to an inexact alarm rather than failing, so the
         worst case is a late warning, never a missed block. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:label="Insta_killer"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- singleTask, not Flutter's default singleTop: the watcher launches this from
             outside our task, and singleTop would let a second copy stack behind the
             first. Repeated opens must land on the one Gate, via onNewIntent. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Enforcement. Event-driven, scoped to Instagram by the config below, and
             exempt from the background-activity-launch restrictions that stop a plain
             foreground service opening the Gate. -->
        <service
            android:name=".ForegroundWatcher"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- The VIP probe. No iOS equivalent exists (D-008). -->
        <service
            android:name=".NotificationProbe"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".GrantAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Every app with a launcher entry, so the Blocklist can offer them. Scoped to
             LAUNCHER rather than QUERY_ALL_PACKAGES: we only need apps a person can open,
             which is exactly the set that can be blocked, and it avoids a permission that
             is both broader than the need and a policy landmine if this is distributed.

             Without this, Android 11+ reports every package as "not installed". -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- Used by the Flutter engine's ProcessTextPlugin. -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
