<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Package visibility (Android 11+). Without this, queryIntentActivities and
         getPackageInfo for Instagram return "not installed" even when it is. We declare
         exactly one package rather than QUERY_ALL_PACKAGES, which is both a policy
         landmine and more access than we need. -->
    <queries>
        <package android:name="com.instagram.android" />
    </queries>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Not used by the accessibility path, but requested so the spike can compare it
         against the UsageStatsManager polling alternative. See P0_SPIKE_ANDROID.md. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".SpikeActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The gate. singleTask + excludeFromRecents so repeatedly tripping the block
             does not stack a pile of gate screens in the recents list. -->
        <activity
            android:name=".GateActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- Foreground detection. Event-driven, so detection is effectively instant,
             and scoped by android:packageNames to Instagram alone. -->
        <service
            android:name=".ForegroundWatcher"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- The VIP probe. This is the API iOS has no equivalent of. -->
        <service
            android:name=".NotificationProbe"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
